import "server-only";
import { setDoctorRole } from "@/actions/auth";

// This is a temporary tool file. 
// You can import this function in a temporary page or run it via a script if environment matches.
// Since running ts-node with Next.js env vars is tricky, we'll create a temporary API route or page.

// Let's create a temporary setup page instead.
